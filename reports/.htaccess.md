# This prevents people from directly accessing the reports folder
# But allows PDFs to be downloaded through your PHP pages

<Files "*">
    Order Deny,Allow
    Deny from all
</Files>

# Only allow PDF files to be accessed
<FilesMatch "\.pdf$">
    Allow from all
</FilesMatch>